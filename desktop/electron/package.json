{
  "name": "kodelet-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Electron shell for kodelet serve",
  "main": "build/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && electron .",
    "dev:local": "npm run build && electron . --kodelet-path ../../bin/kodelet",
    "start": "npm run build && electron .",
    "test": "tsx --test src/test/*.test.ts",
    "package": "npm run build && electron-builder --config.mac.notarize=false"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "electron": "^39.8.5",
    "electron-builder": "^26.0.12",
    "tsx": "^4.20.6",
    "typescript": "^5.9.2"
  },
  "build": {
    "appId": "com.jingkaihe.kodelet.desktop",
    "productName": "Kodelet",
    "directories": {
      "output": "dist"
    },
    "files": [
      "build/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../../bin",
        "to": "bin",
        "filter": [
          "kodelet*"
        ]
      }
    ],
    "mac": {
      "category": "public.app-category.developer-tools",
      "target": [
        "dir",
        "zip"
      ]
    },
    "linux": {
      "category": "Development",
      "target": [
        "AppImage",
        "tar.gz"
      ]
    },
    "win": {
      "target": [
        "portable"
      ]
    }
  }
}
