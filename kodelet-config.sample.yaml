mcp:
    execution_mode: code
    code_execution:
        rpc_transport: "http"
    servers:
        playwright:
            command: "npx"
            args:
                - "@playwright/mcp@latest"
                - "--caps=core"
