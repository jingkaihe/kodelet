// example.ts - Example usage of generated MCP tools
// This file is automatically generated by kodelet mcp generate

{{range .Servers}}
// Example: {{.Name}} server
import * as {{.Name}} from './servers/{{.Name}}/index.js';
{{end}}

{{range $server := .Servers}}
async function example{{$server.Name | title}}() {
  console.log("=== {{$server.Name}} Examples ===");
  
  {{range $server.Tools}}
  // {{.Description}}
  // const result = await {{$server.Name}}.{{.FunctionName}}({ /* params */ });
  // console.log(result);
  {{end}}
}

{{end}}

// Run all examples
async function main() {
  {{range .Servers}}
  await example{{.Name | title}}();
  {{end}}
}

main().catch(console.error);
